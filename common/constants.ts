// Game Types
export enum GameType {
    Solo = 'solo',
    Multiplayer = 'multiplayer',
}

// Game Mode
export enum GameMode {
    Classic = 'classic',
    Sprint = 'sprint',
}

// Position
export enum Position {
    First = 'première',
    Second = 'deuxième',
    Third = 'troisième',
}
